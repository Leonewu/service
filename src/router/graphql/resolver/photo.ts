import PhotoController from "../../../controller/PhotoController"

const resolver = {
  Query: {
    // fetchFolders: PhotoController.fetchFolders
  },
  Mutation: {
    // createFolder: PhotoController.createFolder
  }
}

export default resolver
