export default `
  type Photo {
    id: Int
    name: String
    url: String
    folderId: Int
    createdAt: String
    updatedAt: String
  }
  type Query {
    _empty: String
  }
  type Mutation {
    _empty: String
  }
`
