{
  "watch": ["src"],
  "ext": "ts, js",
  "ignore": ["src/**/*.spec.ts", "src/migration/*.ts"],
  "delay": 300,
  "exec": "kill-port --port 9229 && ts-node --inspect src/index.ts",
  "execMap": {
    "ts": "ts-node"
  }
}